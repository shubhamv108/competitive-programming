package code.linkedlist;

import java.util.*;

//11
//3 1 2 3
public class InsertAtEnd
{
    public static void main(String args[])
    {
        System.out.println(Math.pow(10, (int)Math.floor(Math.log10(913))));
        Scanner sc = new Scanner(System.in);

//        int t = sc.nextInt();

//        while(t--!=0)
//        {
            LinkedListFunctions llf = new LinkedListFunctions();
            LLNode head = null;
//            int n = sc.nextInt();
//            while(n--!=0) head = llf.InsertAtEnd(head, sc.nextInt());
//            LLNode midNode = llf.getMiddleNode(head);
//            LLNode secondHalfReverseHead = llf.reverse(midNode.next);//            llf.printList(secondHalfReverseHead);
//            llf.printList(llf.deleteNode(head, 5));
//            System.out.println(llf.isPalindromicList(head));
//            head = new LLNode(1);
//            head.next = new LLNode(3);
//            head.next.next = head;
//            head.next.next = new LLNode(5);
//            head.next.next.next = head.next;
//            llf.removeCycle(head);

//            LLNode h1 = new LLNode(1);
//            h1.next = new LLNode(2);
//            h1.next.next = new LLNode(3);
//            h1.next.next.next = new LLNode(4);
//            LLNode h2 = new LLNode(4);
//            h2.next = new LLNode(5);
//            h2.next.next = new LLNode(6);
//            llf.frontLastRearrange1(h1);
//            llf.printList(h1);
//            llf.printList(llf.mergeSorted(h1, h2));
//            List
//        }
    }
}